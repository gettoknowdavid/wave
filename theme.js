import { createLightTheme, lightThemePrimitives } from 'baseui';

const WaveTheme = createLightTheme(
  { ...lightThemePrimitives, primaryFontFamily: '"Poppins", sans-serif' },
  {},
);

export default WaveTheme;
